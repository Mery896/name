#include <iostream>

int main() {
    int num;
    std::cout;
    std::cin >> num;

    int sum = 0;
    while (num > 0) {
        sum += num % 10; 
        num /= 10;
    }

    std::cout << sum;

    return 0;
}
